import { VirtualOffice } from '@/components/virtual-office/virtual-office';

export const metadata = { title: 'Virtual Office' };

export default function OfficePage() {
  return <VirtualOffice />;
}
